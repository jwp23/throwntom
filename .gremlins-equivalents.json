[
  {
    "file": "internal/engine/engine.go",
    "line": 204,
    "column": 18,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "e.lastCredit<=0 vs <0: at lastCredit=0 the fallthrough formula reduces to workSessionsBlock/n > (workSessionsBlock-0)/n, i.e. X > X, always false — identical to the guard's early return for every workSessionsBlock and longBreakEvery. Proven algebraically, not just by trial inputs."
  },
  {
    "file": "internal/engine/engine.go",
    "line": 269,
    "column": 26,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "elapsed<=0 vs <0 in MeetingCredits: at elapsed=0 the fallthrough formula is int(work/(2*work)), which is 0 for any work>0 by integer division — identical to the guard's 0, for every work. Same equivalence shape as the lastCredit case above."
  },
  {
    "file": "internal/analytics/analytics.go",
    "line": 156,
    "column": 13,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "minutes>0 vs >=0: at minutes=0 both branches add zero to the same field, so the outcome is identical either way. Confirmed empirically: a hand-applied mutation still passes every test."
  },
  {
    "file": "internal/analytics/analytics.go",
    "line": 187,
    "column": 18,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "value<0 vs <=0: both the \"valid, credit 0\" and \"invalid, reject\" paths return int 0 at value=0, so the mutation is mathematically unobservable."
  },
  {
    "file": "internal/analytics/analytics.go",
    "line": 281,
    "column": 14,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "streak>longest vs >=longest: on a tie, assigning longest = streak is idempotent since they are already equal, so no test can distinguish the mutation from correct code."
  },
  {
    "file": "internal/pomodoro/timer.go",
    "line": 408,
    "column": 24,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "pausedRemaining<0 vs <=0 clamping to zero: at pausedRemaining=0 exactly, the clamp assigns 0 to a field that is already 0, so the observable result is identical whether the branch fires or not. Confirmed empirically: a hand-applied mutation still passes the full internal/pomodoro suite."
  },
  {
    "file": "internal/pomodoro/timer.go",
    "line": 546,
    "column": 7,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "formatRemaining's d<0 vs <=0 clamping to zero: same shape as pausedRemaining above, at d=0 the clamp assigns 0 to a duration that is already 0. Confirmed empirically: a hand-applied mutation still passes the full suite."
  },
  {
    "file": "internal/pomodoro/timer.go",
    "line": 572,
    "column": 13,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "elapsedSincePhaseStartLocked's elapsed<0 vs <=0 clamping to zero: same shape as pausedRemaining above, at elapsed=0 the clamp assigns 0 to a duration that is already 0. Confirmed empirically: a hand-applied mutation still passes the full suite."
  }
]
