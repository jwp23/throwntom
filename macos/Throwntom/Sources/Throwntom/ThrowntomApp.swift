import SwiftUI
import ThrowntomClient

@main
struct ThrowntomApp: App {
    var body: some Scene {
        MenuBarExtra("Throwntom") {
            Text("Throwntom")
        }
        .menuBarExtraStyle(.window)
    }
}
